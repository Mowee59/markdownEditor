import styled from "styled-components";

const StyledVerticalDivider = styled.div`
  border-left: 1px solid ${(props) => props.theme.border};
  height: 4rem;
  margin: 0 2.4rem;
`;

export default StyledVerticalDivider;